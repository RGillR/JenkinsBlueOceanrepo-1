pipeline {
   agent any
   
   environment {
       DEMO='2.1'
       RELEASE='demo2 2.1'
   }

   stages {
        stage('Dev') {
            environment {
            LOG_LEVEL='DEv2.0'
            }
            steps {
            echo "This is build number= $BUILD_NUMBER of demo = $DEMO and $RELEASE and log level= $LOG_LEVEL"
       
            }
      }

        stage('TEST') {
            steps{ 
                echo "I am in stage 2"


            }
        }

        stage('build') {

            parallel {
                stage  ('first stage of the build section_paralel-1') {
                    steps {
                            echo "stage name = $STAGE_NAME"
                    }
                }

                stage  ('2nd stage of build section_parallel')    {
                    steps {
                        echo "stage name = $STAGE_NAME"
                    }
                }
            } 
        }

     stage('Deploy'){
            
            input{
                message 'Deploy?'
                ok 'Yes, please deploy'


            
                parameters {
                    string (name: 'Target_enviornment', defaultValue: 'PROD', description: 'Target development enviornment')
                }
            }

            steps {
                     echo 'this is the deploy stage '
                     echo "this is target = $Target_enviornment"
                

                         
                    }           
        }



        }
     
      
    post {
        always {
            echo ' prints whether deploy happened or not'
        }
    }
}    
   
